chrome.action.onClicked.addListener(async tab=>{const payload={title:tab.title||'',url:tab.url||'',type:'bookmark'};await chrome.storage.local.set({latestDeepDiveCapture:payload});const target=`https://oh-tell-me-more.vercel.app/?capture=${encodeURIComponent(JSON.stringify(payload))}`;chrome.tabs.create({url:target});});
